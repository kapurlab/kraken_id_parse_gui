fastapi>=0.115.0
uvicorn[standard]>=0.33.0,<0.34.0
aiofiles>=24.1.0
jinja2>=3.1.0
pydantic>=2.0.0
# The upload endpoints use fastapi.UploadFile, and FastAPI refuses to import an
# app that declares one unless python-multipart is installed. Working envs had
# it only as an accidental dependency of conda's fastapi package — declare it,
# so a pip-built or pip-repaired env can actually start.
python-multipart>=0.0.9
